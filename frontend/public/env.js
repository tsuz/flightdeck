// Default runtime config (used in dev and as a fallback).
// In the Docker image this file is regenerated at container start from the
// WS_URL environment variable. An empty value means "auto-detect from the page".
window.__ENV__ = { WS_URL: "" };
